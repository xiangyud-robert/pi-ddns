{
  "name": "pi-ddns-lambda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node24 --outfile=dist/handler.js --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-route-53": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.9.3"
  }
}
